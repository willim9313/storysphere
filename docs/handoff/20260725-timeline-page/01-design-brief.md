# 時間軸頁 UX 強化 需求書（交付 Claude Design）

**日期**：2026-07-25
**頁面**：`/books/:bookId/timeline`
**配套計劃**：`docs/plans/20260725-timeline-page-enhancements.md`（工程分期與成本，設計側不需讀完）
**前一輪設計**：`docs/plans/20260519-timeline-page-redesign.md`（V2，已落地為現況）

⚠️ 請掛在**既有的 StorySphere Claude Design 專案**下進行（token 系統、focus ring、
Warm/Ink 雙主題等契約已在該專案內），不要另開新專案。
設計完成後以 **`.dc.html` canvas** 交回，開發端依 canvas 實作（不依 prose 重新詮釋）。

---

## 1. 背景與範圍

時間軸頁 V2 已落地（三視圖卡 + 卡片化節點 + 矩陣散點圖）。2026-07-25 的實測發現：
**資訊架構（三種看法）是對的，但畫面沒有把這頁的核心命題說出來**——
工具列說不清楚自己在幹嘛、篩選只能淡化不能收斂、長書無法導覽、
80% 的畫面是留白、三套色彩系統疊在一起卻都不明顯、缺少時間軸才做得到的角色維度。

> **本次為全頁視覺重做**（2026-07-25 定案）。V2 的視覺語言**不需要沿用**，
> 硬約束只有三條（§5.1–§5.3）。§4 是已知痛點清單，不是工作範圍的邊界。

### 1.1 涉及畫面／元件（**全部開放重新設計**）

> **本次是全頁視覺重做**，不是局部修補。V2（2026-05-19）建立的視覺語言
> **不需要沿用**——包含卡片版面、圖示、色帶編碼、面板層次。
> 下表是**元件清單與現況說明**，不是保留清單。

| 元件 | 位置 | 現況 / 注意 |
|------|------|-------------|
| `Toolbar` | `TimelinePage.tsx:682` | ⭐ §4.0 最高優先。右半邊按鈕外觀是 CSS bug（§4.0a），**沒有既有語言可沿用** |
| `FilterSheet` / `FilterChip` | `TimelinePage.tsx:982` / `958` | 5 分區 chip；角色分區有 67 顆 |
| `TimelineCanvas` | `TimelinePage.tsx:1156` | ⭐ §4.1b 構圖問題（八成留白）。**整體構圖開放重想** |
| `ChapterBand` | `TimelinePage.tsx:1389` | 章節分組容器 |
| `EventCard` | `TimelinePage.tsx:1453` | ⭐ **畫面上出現 62 次，是這頁的視覺性格本身**。現況：左右色帶雙編碼 + NarrativeIcon + K/S 標籤 + pill 溢出 `+N`。**八成卡片資訊稀疏**（§7.3）——開放重新設計 |
| `ParallelGroup` | `TimelinePage.tsx:1414` | 雙線紫框 + 「⤳ 並行支線」 |
| `MatrixCanvas` | `components/timeline/MatrixCanvas.tsx` | 視覺形式全開放；**只有軸編碼是硬約束**（§5.3） |
| `EventDetailPanel` | `TimelinePage.tsx:1562` | 360px 固定。**內容層次開放**——注意 S21（49/62 未分析）才是主導狀態 |
| `MiniTimeline` / `MiniRow` | `TimelinePage.tsx:1720` / `1792` | 開放。⚠️ 命名同時由工程端修正（§2.5），請用「上下文位置」語彙 |
| `AccordionSection` / `EepBody` / `CausalityBody` / `ImpactBody` | `TimelinePage.tsx:1838`+ | 開放（三個 accordion 目前視覺權重一致，無層次） |
| `GenettBanner` / `GenettStructureChip` | `TimelinePage.tsx:549` / `609` | 開放；注意 S10「不可用」才是常態（§2.3） |
| `LockedView` | `TimelinePage.tsx:499` | 2026-07-25 落地。視覺開放，**語意固定**（無 rank 時不得渲染會誤導的視圖） |
| `TimelineOnboardingHero` | `components/timeline/TimelineOnboardingHero.tsx` | 開放（三步引導卡） |
| `NarrativeIcon` | `components/timeline/NarrativeIcon.tsx` | 開放（5 種自繪圖示：present / flashback / flashforward / parallel / unknown） |
| **新增**：角色軌跡泳道 | — | ⭐ **新功能**（§4.4） |

### 1.2 明確不在範圍

- **深色主題** —— 只有 Warm（暖色，預設）與 Ink（黑白墨色），兩套皆淺底。
- **手機版（< 768px）** —— 本頁是桌面分析工具，不做 mobile 退化。
  RWD 只需涵蓋 **1440 / 1280 / 1024** 三個桌面寬度（§4.3）。
- **三視圖的既有概念**（章節順序 / 故事時序 / 矩陣）—— 不推翻、不合併。
  三者各自的**視覺呈現全部開放**，但「這頁提供這三種看法」的資訊架構維持。
- **後端 API schema、rank 計算演算法、Genette 演算法、task polling 機制**。
- **全站 chat FAB** —— 不屬本頁，但它壓在畫布資料區上（見 §6b），請把佔位納入考量。

---

## 2. 資料事實（設計的基礎，勿臆造）

以 `sample-payloads/timeline.json`（真實 #13a 回應）為準。實測數字：

| 欄位 | 實測分布 | 對設計的意義 |
|------|----------|--------------|
| 事件總數 | **62**，橫跨 Ch.1–7 | 每章 6–11 個，章節帶寬度不均 |
| `title` 長度 | 6–17 字（中位 **12**） | 可用真實字串排版，勿用 lorem |
| `description` 長度 | 33–77 字（中位 56） | 詳情面板概要區 |
| `participants` | 中位 **4**，但**有一筆 24 人** | 卡片顯示 2 + `+N`；詳情面板要能排 24 顆 pill |
| 不重複角色 | **67 位** | 篩選面板的角色 chip 會有 67 顆（現有搜尋框） |
| `location` | **0 / 62（全空）** | ⚠️ 地點維度在真實資料中不存在；篩選的「地點」分區會整區隱藏 |
| `eventType` | **8 種**：plot 20 / revelation 16 / conflict 12 / meeting 7 / alliance 3 / battle 2 / turning_point 1 / other 1 | 篩選 chip 8 顆，分布極不平均 |
| `eventImportance` | KERNEL 10 / SATELLITE 3 / **null 49** | ⚠️ **八成事件的 K/S 標籤顯示「·」**——這正是 §4.0 要解的問題 |
| EEP 覆蓋率 | **13 / 62（21%）** | 詳情面板多數時候是「未分析」態 |

> ⚠️ 示範 payload 的人名／章號／文字僅為**結構參考**，設計中的排版請照真實字串長度分布，
> 但實作一律即時從真實圖譜計算、**不寫死示範值**。

### 2.1 `chronologicalRank` 的現實：**算完之後仍有 16% 是 null**

`chronological_rank`（0.0→1.0，事件在**故事時間**中的位置）**可以是 `null`**，
代表「該事件排不進故事時序」。這不是例外，是常態：

| | rank 非空 | rank = null |
|---|---|---|
| 跑「重新計算時序」**之前** | 0 / 62（0%） | **62（100%）** |
| 跑「重新計算時序」**之後**（2026-07-25 實跑） | 52 / 62（84%） | **10（16%）** |

**算完了還是有 10 筆排不進去。** 所以「未排序事件」不是一個過渡狀態，
而是**穩定存在的一類**。任何新視圖都必須先設計好它們放哪。
矩陣視圖現有的解法是 degraded row（Y = -0.1）+「⤵ 未排序事件」象限標籤，可沿用該語彙。

`sample-payloads/timeline-computed.json` 就是算完之後的真實回應（52 有 / 10 無）。

### 2.2 ⚠️ 畫面上一條連線都不會有：199 條關係全部被丟棄

實跑後 `temporalRelations` 有 **200 筆**，但：

| 類型 | 筆數 | 現況處理 |
|------|------|----------|
| `before` | **199** | ⚠️ **全部丟棄**——畫布只收 `CAUSES` |
| `unknown` | 1 | 丟棄 |
| `CAUSES` | **0** | 畫布唯一會畫的類型，實際上一筆都沒有 |

也就是說：**現況設計的 CAUSES 連線樣式（實線／虛線、箭頭、confidence 分級）
在真實資料上永遠不會出現，而手上有 199 條沒被使用的時序關係。**

這對設計的意義：不要假設「連線」是稀有的裝飾。若把 `before` 納入呈現，
畫面上會同時有 199 條線（62 個節點），**線的密度管理會是主要的設計問題**。

### 2.3 ⚠️ Genette 的成功態在真實資料上到不了

「Genette 分析」有一道前置門檻：`storyTimeHint`（事件的故事時間線索文字，
如「此時天尚未亮」「卯時中」）覆蓋率需 **≥ 60%**。

實測：**9 / 62（14.5%）**，且跑完時序計算**沒有改善**（兩者資料來源不同）。

因此：
- **S10「Genette 覆蓋率不足」是這頁的常態，不是邊緣狀態**——請把它當主要狀態設計
- **S11（成功 banner + 結構 chip）、S27（displacement 色帶）、S30（矩陣 Genette 著色）
  在真實資料上都到不了**，只能用構造樣本呈現

### 2.4 `narrativeMode` 實際上只有一種值

實測 **62 / 62 全部是 `present`**。倒敘（flashback）／預敘（flashforward）／
並行（parallel）三種樣式與 `ParallelGroup` 群組樣式**在真實資料上都不出現**。

→ 這三者 + §2.3 的 displacement，共同構成 `sample-payloads/timeline-constructed.json`
（**手工構造，明確標示**）的存在理由。設計這些樣式時請用那份，
但**不要以為真實畫面會長那樣**。

### 2.5 兩個名詞的正確語意（文案不得誤用）

- **「故事時序」（fabula）vs「章節順序」（sjuzhet）**：前者是事件實際發生的先後，
  後者是作者敘述的先後。整頁的核心價值就是這兩者的落差。
- **詳情面板的「上下文位置」不是因果、也不是時序**：它的定義是「與本事件**共享參與者**、
  且位於**較早／較晚章節**的事件」。**文案一律不得稱之為「因果」或「前驅／後續」**
  （事件分析頁已於 PR #18 正名，本頁跟進）。

---

## 3. 狀態矩陣（設計必須全部涵蓋）

這頁的狀態比表面上多。**請逐格確認你的設計在該狀態下成立**：

### 3.1 頁面層級

| # | 狀態 | 觸發條件 | 現況 |
|---|------|----------|------|
| S1 | 首次載入 | 無快取 | 全頁 spinner |
| S2 | **切換視圖載入** | 切換章節順序↔故事時序 | 保留前一份資料 + 畫布上方浮出膠囊指示器（2026-07-25 落地） |
| S3 | 載入失敗 | API error | `ErrorMessage` |
| S4 | **無事件** | `events.length === 0` | `TimelineOnboardingHero`（三步引導卡） |
| S5 | **有事件、無 rank** | `hasChronologicalRanks === false` | QualityBanner + **後兩張視圖卡 disabled** + `LockedView`（2026-07-25 落地） |
| S6 | 正常 | 有事件有 rank | 三視圖可用 |

### 3.2 工具列

| # | 狀態 | 現況 |
|---|------|------|
| S7 | 重新計算中 | 按鈕 disabled + spinner「計算中…」 |
| S8 | Genette 未跑 | 按鈕常態 |
| S9 | Genette 跑中 | disabled + spinner |
| S10 | **Genette 覆蓋率不足** | 按鈕 `opacity: 0.65` + warning dot，原因**只在 tooltip**（實測：「故事時間提示覆蓋率 15%，低於 60% 閾值」）→ §4.1 要解 |
| S11 | Genette 成功 | banner + 結構 chip（`linear` 綠／`partially_linear` 琥珀／`non_linear` 藍紫）|
| S12 | Genette 失敗 | error banner |
| S13 | banner 已關閉但 chip 保留 | data 與 banner 解耦 |
| S14 | 品質 chip | `hasRanks` 時顯示「█████░ 65% 已分析」 |

### 3.3 篩選

| # | 狀態 | 現況 |
|---|------|------|
| S15 | 無篩選 | — |
| S16 | 有篩選 | ActiveFilters bar（可單獨移除 + 全部清除）+ 按鈕上的數字 badge |
| S17 | FilterSheet 開啟 | 下拉面板，5 分區 chip |
| S18 | **篩選結果為空** | ⚠️ **現況未定義**——全部卡片變灰，畫面看起來像壞掉。§4.2 要解 |

### 3.4 事件詳情面板

| # | 狀態 | 現況 |
|---|------|------|
| S19 | 未選取 | 面板不掛載，畫布佔滿 |
| S20 | 選取・已分析 | hero + 主題意義 + 概要 + 上下文位置 + 三 accordion |
| S21 | 選取・**未分析**（62 筆中的 49 筆） | 無 hero 主題意義、accordion 皆空 + 「前往深度分析頁」CTA |
| S22 | 分析載入中 | 各區塊 spinner |
| S23 | 上下文位置無資料 | 「尚未分析」提示 |

### 3.5 畫布

| # | 狀態 | 現況 |
|---|------|------|
| S24 | 橫向 layout | 章節帶水平排列，次軸置中 |
| S25 | 直向 layout | 章節帶垂直堆疊，水平置中 |
| S26 | 並行事件群組 | 雙線紫框 + 「⤳ 並行支線」 |
| S27 | Genette displacement 色帶 | 卡片右側 3px（倒敘藍／預敘琥珀） |
| S28 | CAUSES 連線 | confidence ≥ 0.8 實線 / < 0.8 虛線；< 0.5 不畫 |
| S29 | 矩陣・narrativeMode 著色 | 預設 |
| S30 | 矩陣・Genette 著色 | toggle 開啟，圖例同步切換 |
| S31 | 矩陣・框選中 | 未選中 dot opacity 0.15 |

> **S26 / S27 / S28 在目前的種子書中都不會出現**（全部事件都是 `present`、零關係）。
> 設計時請以 `sample-payloads/timeline-computed.json` 為準，那份含真實的倒敘／預敘資料。

---

## 4. 設計需求清單

> ⚠️ **這是一份「已知問題清單」，不是工作範圍的邊界。**
> 本次為全頁視覺重做——下面 7 項是實測找出的具體痛點，但**不代表沒列到的地方就該維持原樣**。
> 版面節奏、資訊密度、字級層次、顏色的角色分配、卡片形態，都在範圍內。

### 4.-1 【最高】這頁的視覺任務 ⭐ 先讀這節

**一句話**：這頁要讓人看出「**作者敘述的順序**」與「**故事實際發生的順序**」之間的落差。

現況的問題不在缺功能，而在**畫面沒有把這件事說出來**：

- 62 張卡片視覺權重幾乎一致，掃過去看不出哪些重要（**八成卡片的 K/S 標籤是「·」**，§7.3）
- 顏色同時編碼 narrativeMode（左色帶）、displacement（右色帶）、entity type（pill），
  **三套色彩系統疊在同一張小卡上**，但真實資料裡前兩者幾乎都是單一值（§2.3 / §2.4）
  ——也就是**大部分的色彩負擔沒有換來資訊**
- 畫面 80% 是留白（§4.1b），而 62 張卡裡只看得到 7 張

**要什麼**：重新決定這頁的**視覺重心與資訊層級**——什麼該搶眼、什麼該退到背景、
顏色應該編碼什麼（而不是編碼三種東西然後都不明顯）、卡片在密集排列下該是什麼形態。
`EventCard` 出現 62 次，它就是這頁的性格；請把它當主要設計對象，不是既有零件。

### 4.0 【最高】工具列右半邊：說不清楚自己在幹嘛 ⭐

**使用者原話**：「這邊當初設計上好像沒有寫得很清楚在幹麻。」

```
現況：[⇄ ↕]  [☰ 篩選]  [↻ 重新計算時序]  [⎇ Genette 分析]
```

四個問題：

**(a) ⚠️ 現況的按鈕外觀不是設計決策，是 CSS 意外**
`timeline.css:22` 的 `.tl button`（特異性 0,1,1）蓋掉了 `.tl-btn`（0,1,0）的
`border` 與 `background`。實測 computed：`border: none`、`background: transparent`。
**三顆按鈕靜止時都是裸文字**，底色只在 hover 時出現（`:hover` 特異性夠高才贏回來）。
→ **請直接決定按鈕該長什麼樣，不必揣摩「原本為什麼長這樣」**；開發端負責解特異性衝突。
（這也是為什麼 QualityBanner 的 CTA 看起來像按鈕、工具列這幾顆卻不像。）

**(b) layout 切換零文字**
只有兩個箭頭圖示（`ArrowLeftRight` / `ArrowUpDown`）+ tooltip。而這兩個圖示在多數
產品裡讀起來是「排序／交換」而非「排列方向」。
對比左半邊：三張視圖卡**每張都有一行解釋副標**。同一條工具列，左半在教學、右半零說明。

**(c) 三種性質的操作同一視覺權重**

| 控制項 | 性質 | 代價 | 可逆 |
|--------|------|------|------|
| layout ⇄↕ | 檢視選項 | 免費即時 | ✅ |
| 篩選 | 資料篩選 | 免費即時 | ✅ |
| **重新計算時序** | 觸發 LLM | **數分鐘 + token** | ❌ 覆寫既有 rank |
| **Genette 分析** | 觸發 LLM | **數分鐘 + token** | ❌ |

後兩者的成本只寫在 tooltip，且**沒有確認步驟**。全站慣例是有的：
事件頁與角色頁都用 `ConfirmDialog`，事件頁批次生成還顯示「預估約 7 分鐘 ·
已分析的事件會自動跳過」。**時間軸頁是唯一沒跟上的。**

**(d) 「Genette」是專有名詞，零解釋**
使用者看到一顆灰掉、沒說為什麼灰、也不知道 Genette 是誰的按鈕（S10）。

**要什麼**：分組（檢視選項 / 資料操作 / 昂貴計算）、昂貴操作要有成本感與確認、
停用要在畫面上說明原因、layout 要看得懂、Genette 要有一句人話。

### 4.1 【高】篩選：只能淡化，不能收斂

現況套用篩選只是把不符合的卡片加 `.dim`（變灰），**卡片仍佔位**。
62 張卡、67 位角色的情況下，使用者得在一片灰卡裡目視找亮的。

**要什麼**：
- 「符合 N / 共 M 筆」計數
- 「只顯示符合項」與「淡化其餘」兩種模式（**兩者都有用**：淡化保留上下文與位置感，
  收斂讓長書可讀。請設計切換方式，不要二選一砍掉）
- **S18 篩選結果為空的狀態**（現況未定義）

### 4.1b 【高】橫向畫布：八成的畫面是空白，而且看不出還有內容 ⭐

見 `screenshots/tl-warm-01-narrative.png`。在 1440×900 下：

- **上下各約 300px 全空**。橫向 layout 只有一列章節帶，而
  `.tl-canvas-inner:not(.vertical) { align-items: center }`（UI_SPEC §3.7 的「次軸置中」決策）
  把它垂直置中，內容只佔畫面高度約 **20%**。
- **畫面上只看得到 Ch.1 的 7 張卡**，但實際有 **62 張、scrollWidth 12390px**。
  **沒有任何捲動提示**——使用者不知道右邊還有 55 張卡、還有 6 章。
- 品質 chip「21% 已分析」與旁邊的按鈕一樣是裸文字，分不出哪個可點。

**要什麼**：**畫布的整體構圖開放重想**——留白拿來承載導覽（§4.2 的 ruler／minimap
是天然人選）、改變置中策略、改變卡片的排列密度與形態、甚至重新決定「橫向單列」
是不是對的骨架，都可以。
**「內容只佔 20% 高度且看不出還有更多」是本次最該解掉的第一印象問題。**

### 4.2 【高】長書導覽

62 事件橫向排列，畫布 scrollWidth 實測 **12390px**。現況只能靠滑鼠拖。

**要什麼**：章節 ruler 或 minimap、「跳到第 N 章」、鍵盤 `←→` 在事件間移動、
`Esc` 關閉面板、面板開啟時的 focus 落點。

### 4.3 【中】RWD 與詳情面板

現況 `timeline.css` 1304 行只有一個 `@media (max-width: 640px)`。
面板固定 360px、不可摺、不可調寬，在 1280 視窗就把橫向畫布壓得很窄。

**要什麼**：**1440 / 1280 / 1024** 三個寬度下的行為（面板摺疊？覆蓋？可拖曳調寬？）。
**不需處理 < 768px。**

### 4.4 【高】角色軌跡泳道（新視圖）⭐ 新功能

選 1–3 位角色 → 時間軸切成泳道，呈現：誰在哪些事件出現、哪段完全缺席、誰與誰同框。

- **為什麼**：知識圖譜頁看「關係是什麼」，時間軸應該看「關係**何時**發生」。
  現況兩頁的差異只有排版，泳道才是時間軸不可取代的理由。
- **資料已備齊**：`participants` 已含 id / name / type，零後端改動。
- ⚠️ **必須以「章節」而非 `chronological_rank` 定位**（rank 可能全空，見 §2.1）。
- **未決**：這是**第四張視圖卡**，還是既有視圖上的**疊加模式**？請在設計中給出建議。

---

## 5. 設計約束

**本次是全頁視覺重做，硬約束只有三條**（§5.1–§5.3）。其餘為執行規範。

### 5.1 【硬】token：不新增，且不得改動既有值

- 顏色／字體一律 CSS variable（`var(--*)`），**禁止硬編碼色碼**。
- `frontend/src/styles/tokens.css` 是唯一來源（已附為 `02-tokens.css`）。
- ⚠️ **特別注意 `--narrative-*` 是跨頁共用的**：`present` / `flashback` / `flashforward` /
  `parallel` / `unknown` 這組 token 同時被**角色分析頁**（`ArcPane`）與
  **事件分析頁**（`event-analysis.css`）使用，兩頁都剛翻新完。
  **改動 token 的值會同時破壞那兩頁。**
  → 但**時間軸頁怎麼使用它們完全開放**：色帶粗細／面積／改用底色或圖示編碼／
  是否降低顏色的角色，都可以重新決定。約束的是「值」，不是「用法」。
- 若確有必要新增 token，請在 canvas 中明確標示並附理由（開發端會同步
  `docs/DESIGN_TOKENS.md` 對照表）；但請把它當例外，不是預設手段。

### 5.2 【硬】雙主題 Warm + Ink，皆淺底

**無深色主題。** 兩套下皆需成立，`.dc.html` 請兩套都出。

### 5.3 【硬】矩陣視圖的軸編碼

X = 章節（離散）、Y = `chronological_rank`（0→1）、未排序事件在 `-0.1` 的 degraded row、
45° 對照線代表「敘事順序 = 故事順序」。

**這是這張圖的資訊本體，不可改。** 但視覺形式全開放：
點的形狀與大小、密度處理（§7.5 的 overplotting）、格線、象限標籤、圖例位置、
軸標籤、直方圖的存廢——都可以重新設計。

### 5.4 執行規範（非設計限制）

- 文案 i18n（zh 為主），用 `i18n/analysis.zh-TW.timeline.json` 的真實字串排版，勿用 lorem ipsum。
- 任務狀態一律輪詢，無 WebSocket；勿假設即時 push。
- `.tl-*` class 前綴；樣式檔為 `frontend/src/styles/timeline.css`。
- 與事件分析頁、角色分析頁**在 token 層級**保持一致即可；
  **版面語言不需要跟它們一樣**——時間軸的資料形狀（62 個節點橫跨 7 章）本來就不同。
- 現有 UI 規格 `docs/UI_SPEC.md §3.7`；重設計後由開發端回寫。
- 設計完成後以 **`.dc.html` canvas** 交回。

---

## 6. 驗收清單（設計交付時自查）

**視覺重做**
- [ ] 掃過畫面能看出**哪些事件重要**（現況 62 張卡權重幾乎一致，八成 K/S 是「·」）
- [ ] 顏色編碼的東西**變少但變明顯**（現況三套色彩系統疊在同一張小卡上，且多數是單一值）
- [ ] `EventCard` 已當作主要設計對象重新決定形態，而非沿用 V2 零件
- [ ] Warm 與 Ink 兩主題皆出，且 `--narrative-*` 的**值未被改動**（跨頁共用，§5.1）

**已知痛點**
- [ ] **工具列右半邊**：四個控制項有明確分組，昂貴操作與免費操作在視覺上可區分
- [ ] 昂貴操作（重新計算時序 / Genette）有成本感 + 確認步驟，且停用時**在畫面上**說明原因
- [ ] layout 切換不看 tooltip 也知道在切什麼
- [ ] 「Genette 分析」有一句不需文學理論背景也讀得懂的說明
- [ ] **橫向畫布不再有八成留白**，且看得出右邊還有內容（§4.1b）
- [ ] **篩選**：符合筆數 + 「只顯示符合項／淡化其餘」兩模式 + **結果為空的狀態（S18）**
- [ ] **長書導覽**：12390px 寬的畫布有章節層級的導覽手段
- [ ] 矩陣視圖的 overplotting 有解（同章 11 顆事件疊成一柱）
- [ ] 「敘事順序 ≈ 故事順序」（偏離很小）看起來是個結論，不是像沒載入資料
- [ ] **泳道**：以章節定位（非 rank）；已回答「第四張卡 vs 疊加模式」
- [ ] 面板在 1440 / 1280 / 1024 三個寬度下皆成立（不需 < 768px）
- [ ] **狀態矩陣 §3 的 S1–S31 逐格檢查**，特別是 S5（無 rank）、S18（篩選為空）、S21（未分析事件）
- [ ] `rank === null` 的事件在每個新視圖中都有明確位置
- [ ] 全文無「因果」「前驅／後續」字樣指涉「上下文位置」（§2.5）
- [ ] 以 `.dc.html` canvas 交付

---

## 6b. 截圖對照（`screenshots/`，皆為 2026-07-25 實跑時序計算後的真實畫面）

| 檔案 | 內容 | 值得注意 |
|------|------|----------|
| `tl-warm-01-narrative.png` | 章節順序（橫向） | ⭐ **上下各約 300px 空白、只看得到 Ch.1 的 7/62 張卡、無捲動提示**（§4.1b）；工具列按鈕無邊框而 layout 切換組**有**灰底容器，不一致一目了然（§4.0a） |
| `tl-warm-02-chronological.png` | 故事時序（橫向） | 同上骨架，卡片改依 rank 排序 |
| `tl-warm-03-matrix.png` | 矩陣視圖 | ⭐ 52 筆有 rank 的散點 + **底部 10 筆灰色未排序事件**；同章事件在離散 X 軸上**垂直重疊成柱狀**（Ch.2 疊了 11 顆）→ overplotting 是真問題；圖例方框壓在 Ch.7 資料上；頂部「事件密度」直方圖標籤被切掉 |
| `tl-warm-04-detail-panel.png` | 詳情面板開啟 | 360px 固定寬，畫布被壓縮 |
| `tl-warm-05-filter-sheet.png` | 篩選面板展開 | 8 種 eventType chip + 67 位角色的 chip 清單 |
| `tl-ink-01-narrative.png` | Ink 主題・章節順序 | 兩主題皆需成立 |
| `tl-ink-02-matrix.png` | Ink 主題・矩陣 | 同上 |

> 所有截圖右下角的圓形浮動按鈕是**全站 chat FAB**，不屬本頁範圍，但它**壓在畫布資料區上**，
> 設計新視圖時請把它的佔位納入考量。

---

## 7. 給設計側的已知現象

1. **現況截圖中工具列按鈕沒有邊框與底色是 bug，不是設計**（§4.0a）。
2. **地點維度在真實資料中是空的**（0/62），篩選的地點分區實際上不會出現。
3. **八成事件未分析**（49/62 無 importance），所以卡片上大量顯示「·」而非 K/S。
   工程端會另加一個「已分析／未分析」的資料旗標，**這個區分怎麼呈現請一併設計**
   ——它是重要度之外，卡片上第二個真正有訊息量的維度（§4.-1）。
4. **並行群組、倒敘色帶、CAUSES 連線在種子書中都不出現**——這些樣式請以
   `timeline-constructed.json`（手工構造，已標示）為準，**不要以現況截圖推論它們不存在**。
5. **矩陣視圖的 overplotting**：X 軸是離散章節，同章事件全部疊在同一條垂直線上
   （實測 Ch.2 疊了 11 顆）。現況靠 dot 半徑與 opacity 硬撐，密集章節會糊成一柱。
6. **本書的敘事順序幾乎等於故事順序**：矩陣散點大致貼著 45° 線。
   這頁的核心賣點（sjuzhet 與 fabula 的偏離）在這本書上訊號很弱——
   設計時請確保**「偏離很小」也是一個看得懂的結果**，而不是看起來像沒載入資料。
